---
import { LOCALES, type Locale, getLangFromUrl, loadT } from '../../i18n';
import HeaderHome from '../../components/HeaderHome.astro';

export async function getStaticPaths() {
  return (LOCALES as readonly string[]).map((lang) => ({ params: { lang } }));
}

const { lang } = Astro.params as { lang: Locale };
const t = await loadT(lang, ['home', 'header']);

// Función para obtener la URL con el idioma cambiado
function getLangUrl(newLang: Locale): string {
  const currentPath = Astro.url.pathname;
  const pathWithoutLang = currentPath.replace(/^\/(es|en|ca)/, '');
  return `/${newLang}${pathWithoutLang || '/'}`;
}

const langLabels: Record<Locale, string> = {
  es: 'ESP',
  en: 'ENG',
  ca: 'CAT'
};
---

<!DOCTYPE html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" href="/images/favicon.png" />
    <meta name="description" content="New Village Studio. Barre, Pilates, Functional & Yoga in Barcelona. Join the movement." />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={`https://www.newvillagestudio.com/${lang}/`} />
    <meta property="og:title" content="NewVillage | Movement Studio" />
    <meta property="og:description" content="New Village Studio. Barre, Pilates, Functional & Yoga in Barcelona. Join the movement." />
    <meta property="og:image" content="/images/herobg.jpg" />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={`https://www.newvillagestudio.com/${lang}/`} />
    <meta property="twitter:title" content="NewVillage | Movement Studio" />
    <meta property="twitter:description" content="New Village Studio. Barre, Pilates, Functional & Yoga in Barcelona. Join the movement." />
    <meta property="twitter:image" content="/images/herobg.jpg" />

    <style>
      @import url('https://fonts.googleapis.com/css2?family=Oswald:wght@200..700&display=swap');
    </style>
    <title>NewVillage | Movement Studio</title>
  </head>
  <body>
    <div class="page-wrapper">
      <!-- Header -->
      <HeaderHome lang={lang} />

      <!-- Hero Section -->
      <section class="hero-section">
        <div class="hero-background">
          <img src="/images/herobg.jpg" alt="Background" class="hero-bg-image" />
    </div>
    
      <div class="container">
          <div class="hero-content">
            <!-- Texto izquierda -->
            <div class="hero-text">
              <h1 class="hero-title">
                <span class="hero-line-1">{t('hero.title_line1')}</span>
                <span class="hero-line-2">{t('hero.title_line2')}</span>
                <span class="hero-line-3">{t('hero.title_line3')}</span>
              </h1>
        </div>
      </div>
    </div>
    
        <!-- Logo rotado derecha - INCORPORADO SVG INLINE -->
        <div class="hero-logo-container">
          <svg 
            version="1.2" 
            xmlns="http://www.w3.org/2000/svg" 
            viewBox="0 0 610 259" 
            class="logo-rotated-svg"
            preserveAspectRatio="xMidYMid meet"
          >
            <path 
              d="m510.23 0l-80.36 131.62 1.37-131.62h-83.42-16.06l-75.72 124.02 1.29-124.02h-83.42-16.06l-157.85 258.53h101.12l72.79-120.46v120.46h80.72 20.4l72.79-120.46v120.46h4.55 76.17 24.96l156.21-258.53z" 
              fill="#A53716" 
              fill-opacity="0.3"
            />
      </svg>
    </div>
  </section>

      <!-- Disciplines Section -->
      <section class="disciplines-section" id="disciplines">
        <div class="disciplines-container">
          <h2 class="disciplines-title">{t('disciplines.sectionTitle')}</h2>
          <p class="disciplines-subtitle">{t('disciplines.sectionSubtitle')}</p>
          
          <div class="disciplines-carousel-wrapper">
            <button class="carousel-nav-btn carousel-prev mobile-only" aria-label="Previous discipline">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M15 18L9 12L15 6" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </button>
            <div class="disciplines-grid">
            <div class="discipline-item" data-discipline="barre">
              <img src="/images/barre.jpg" alt="Barre" class="discipline-image" />
              <div class="discipline-overlay">
                <h3 class="discipline-overlay-title">{t('disciplines.barreTitle')}</h3>
                <p class="discipline-overlay-tags">{t('disciplines.barreTags')}</p>
                <a href={`/${lang}/plans`} class="discipline-view-more-btn mobile-only">
                  <span>{t('disciplines.viewMore')}</span>
                </a>
            </div>
              <div class="discipline-gradient-overlay">
                <button class="discipline-view-more-btn desktop-only">
                  <span>{t('disciplines.viewMore')}</span>
                  <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M7.5 15L12.5 10L7.5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
                </button>
            </div>
          </div>
            <div class="discipline-item" data-discipline="pilates">
              <img src="/images/pilates.jpg" alt="Pilates" class="discipline-image discipline-image-right" />
              <div class="discipline-overlay">
                <h3 class="discipline-overlay-title">{t('disciplines.pilatesTitle')}</h3>
                <p class="discipline-overlay-tags">{t('disciplines.pilatesTags')}</p>
                <a href={`/${lang}/plans`} class="discipline-view-more-btn mobile-only">
                  <span>{t('disciplines.viewMore')}</span>
                </a>
              </div>
              <div class="discipline-gradient-overlay">
                <button class="discipline-view-more-btn desktop-only">
                  <span>{t('disciplines.viewMore')}</span>
                  <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M7.5 15L12.5 10L7.5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
                </button>
            </div>
          </div>
            <div class="discipline-item" data-discipline="funcional">
              <img src="/images/funcional.jpg" alt="Funcional" class="discipline-image discipline-image-right" />
              <div class="discipline-overlay">
                <h3 class="discipline-overlay-title">{t('disciplines.functionalTitle')}</h3>
                <p class="discipline-overlay-tags">{t('disciplines.functionalTags')}</p>
                <a href={`/${lang}/plans`} class="discipline-view-more-btn mobile-only">
                  <span>{t('disciplines.viewMore')}</span>
                </a>
        </div>
              <div class="discipline-gradient-overlay">
                <button class="discipline-view-more-btn desktop-only">
                  <span>{t('disciplines.viewMore')}</span>
                  <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M7.5 15L12.5 10L7.5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                  </svg>
                </button>
        </div>
      </div>
            <div class="discipline-item" data-discipline="yoga">
              <img src="/images/yoga.jpg" alt="Yoga" class="discipline-image discipline-image-right" />
              <div class="discipline-overlay">
                <h3 class="discipline-overlay-title">{t('disciplines.yogaTitle')}</h3>
                <p class="discipline-overlay-tags">{t('disciplines.yogaTags')}</p>
                <a href={`/${lang}/plans`} class="discipline-view-more-btn mobile-only">
                  <span>{t('disciplines.viewMore')}</span>
                </a>
    </div>
              <div class="discipline-gradient-overlay">
                <button class="discipline-view-more-btn desktop-only">
                  <span>{t('disciplines.viewMore')}</span>
                  <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M7.5 15L12.5 10L7.5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                  </svg>
                </button>
        </div>
            </div>
          </div>

          <!-- Barre Detail Section -->
          <div class="discipline-detail" id="barre-detail">
            <button class="mobile-close-detail mobile-only" aria-label="Cerrar">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M18 6L6 18" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                <path d="M6 6L18 18" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </button>
            <div class="discipline-detail-content">
              <h2 class="discipline-detail-title">{t('disciplines.barreDetailTitle')}</h2>
              <h3 class="discipline-detail-subtitle">{t('disciplines.barreDetailSubtitle')}</h3>
              <p class="discipline-detail-text">{t('disciplines.barreDetailText')}</p>
              
              <div class="discipline-programs">
                <div class="program-box program-box-1">
                  <h4 class="program-title">{t('disciplines.barreProgram1Title')}</h4>
                  <p class="program-text">{t('disciplines.barreProgram1Text')}</p>
            </div>
                <div class="program-box program-box-2">
                  <h4 class="program-title">{t('disciplines.barreProgram2Title')}</h4>
                  <p class="program-text">{t('disciplines.barreProgram2Text')}</p>
          </div>
                <div class="program-box program-box-3">
                  <h4 class="program-title">{t('disciplines.barreProgram3Title')}</h4>
                  <p class="program-text">{t('disciplines.barreProgram3Text')}</p>
            </div>
          </div>
        </div>
        </div>

          <!-- Pilates Detail Section -->
          <div class="discipline-detail" id="pilates-detail">
            <button class="mobile-close-detail mobile-only" aria-label="Cerrar">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M18 6L6 18" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                <path d="M6 6L18 18" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
            </button>
            <div class="discipline-detail-content">
              <h2 class="discipline-detail-title">{t('disciplines.pilatesDetailTitle')}</h2>
              <h3 class="discipline-detail-subtitle">{t('disciplines.pilatesDetailSubtitle')}</h3>
              <p class="discipline-detail-text">{t('disciplines.pilatesDetailText')}</p>
              
              <div class="discipline-programs">
                <div class="program-box program-box-1">
                  <h4 class="program-title">{t('disciplines.pilatesProgram1Title')}</h4>
                  <p class="program-text">{t('disciplines.pilatesProgram1Text')}</p>
              </div>
                <div class="program-box program-box-2">
                  <h4 class="program-title">{t('disciplines.pilatesProgram2Title')}</h4>
                  <p class="program-text">{t('disciplines.pilatesProgram2Text')}</p>
              </div>
                <div class="program-box program-box-3">
                  <h4 class="program-title">{t('disciplines.pilatesProgram3Title')}</h4>
                  <p class="program-text">{t('disciplines.pilatesProgram3Text')}</p>
            </div>
              </div>
              </div>
            </div>

          <!-- Funcional Detail Section -->
          <div class="discipline-detail" id="funcional-detail">
            <button class="mobile-close-detail mobile-only" aria-label="Cerrar">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M18 6L6 18" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                <path d="M6 6L18 18" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
            </button>
            <div class="discipline-detail-content">
              <h2 class="discipline-detail-title">{t('disciplines.functionalDetailTitle')}</h2>
              <h3 class="discipline-detail-subtitle">{t('disciplines.functionalDetailSubtitle')}</h3>
              <p class="discipline-detail-text">{t('disciplines.functionalDetailText')}</p>
          
              <div class="discipline-programs discipline-programs-5">
                <div class="program-row program-row-top">
                  <div class="program-box program-box-1">
                    <h4 class="program-title">{t('disciplines.functionalProgram1Title')}</h4>
                    <p class="program-text">{t('disciplines.functionalProgram1Text')}</p>
              </div>
                  <div class="program-box program-box-2">
                    <h4 class="program-title">{t('disciplines.functionalProgram2Title')}</h4>
                    <p class="program-text">{t('disciplines.functionalProgram2Text')}</p>
              </div>
                  <div class="program-box program-box-3">
                    <h4 class="program-title">{t('disciplines.functionalProgram3Title')}</h4>
                    <p class="program-text">{t('disciplines.functionalProgram3Text')}</p>
            </div>
          </div>
                <div class="program-row program-row-bottom">
                  <div class="program-box program-box-4">
                    <h4 class="program-title">{t('disciplines.functionalProgram4Title')}</h4>
                    <p class="program-text">{t('disciplines.functionalProgram4Text')}</p>
          </div>
                  <div class="program-box program-box-5">
                    <h4 class="program-title">{t('disciplines.functionalProgram5Title')}</h4>
                    <p class="program-text">{t('disciplines.functionalProgram5Text')}</p>
        </div>
        </div>
      </div>
    </div>
      </div>

          <!-- Yoga Detail Section -->
          <div class="discipline-detail" id="yoga-detail">
            <button class="mobile-close-detail mobile-only" aria-label="Cerrar">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M18 6L6 18" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                <path d="M6 6L18 18" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
            </button>
            <div class="discipline-detail-content">
              <h2 class="discipline-detail-title">{t('disciplines.yogaDetailTitle')}</h2>
              <h3 class="discipline-detail-subtitle">{t('disciplines.yogaDetailSubtitle')}</h3>
              <p class="discipline-detail-text">{t('disciplines.yogaDetailText')}</p>
              
              <div class="discipline-programs discipline-programs-2">
                <div class="program-box program-box-yoga-1">
                  <h4 class="program-title">{t('disciplines.yogaProgram1Title')}</h4>
                  <p class="program-text">{t('disciplines.yogaProgram1Text')}</p>
        </div>
                <div class="program-box program-box-yoga-2">
                  <h4 class="program-title">{t('disciplines.yogaProgram2Title')}</h4>
                  <p class="program-text">{t('disciplines.yogaProgram2Text')}</p>
              </div>
            </div>
          </div>
              </div>
            <button class="carousel-nav-btn carousel-next mobile-only" aria-label="Next discipline">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M9 18L15 12L9 6" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                  </svg>
            </button>
          </div>
        </div>
      </section>

      <!-- CTA Section -->
      <section class="cta-section">
        <div class="container" style="height: 100%; display: flex; align-items: center; justify-content: center;">
          <div class="cta-content">
            <h2 class="cta-text">{t('disciplines.ctaSection.text')}</h2>
            <a href="#" class="cta-button">{t('disciplines.ctaSection.button')}</a>
            </div>
    </div>
  </section>

      <!-- Local Image Section -->
      <section class="local-image-section">
        <img src="/images/local.jpg" alt="NewVillage Local" class="local-image" />
      </section>

      <!-- FAQs Modal -->
      <div id="faqs-modal" class="faqs-modal">
        <div class="faqs-modal-content">
          <button class="faqs-modal-close" aria-label="Close modal">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <line x1="18" y1="6" x2="6" y2="18"></line>
              <line x1="6" y1="6" x2="18" y2="18"></line>
            </svg>
              </button>
          
          <h2 class="faqs-title">{t('faqs.title')}</h2>
          <div class="faqs-accordion">
            <div class="faq-item">
              <button class="faq-question">
                <span>{t('faqs.q1')}</span>
                <svg class="faq-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M6 9l6 6 6-6"/>
                </svg>
              </button>
              <div class="faq-answer">
                <p>{t('faqs.a1')}</p>
              </div>
            </div>

            <div class="faq-item">
              <button class="faq-question">
                <span>{t('faqs.q2')}</span>
                <svg class="faq-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M6 9l6 6 6-6"/>
                  </svg>
              </button>
              <div class="faq-answer">
                <p>{t('faqs.a2')}</p>
          </div>
        </div>

            <div class="faq-item">
              <button class="faq-question">
                <span>{t('faqs.q3')}</span>
                <svg class="faq-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M6 9l6 6 6-6"/>
                </svg>
              </button>
              <div class="faq-answer">
                <p>{t('faqs.a3')}</p>
      </div>
    </div>

            <div class="faq-item">
              <button class="faq-question">
                <span>{t('faqs.q4')}</span>
                <svg class="faq-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M6 9l6 6 6-6"/>
            </svg>
              </button>
              <div class="faq-answer">
                <p>{t('faqs.a4')}</p>
      </div>
    </div>

            <div class="faq-item">
              <button class="faq-question">
                <span>{t('faqs.q5')}</span>
                <svg class="faq-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M6 9l6 6 6-6"/>
                </svg>
              </button>
              <div class="faq-answer">
                <p>{t('faqs.a5')}</p>
        </div>
    </div>

            <div class="faq-item">
              <button class="faq-question">
                <span>{t('faqs.q6')}</span>
                <svg class="faq-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                  <path d="M6 9l6 6 6-6"/>
                </svg>
              </button>
              <div class="faq-answer">
                <p>{t('faqs.a6')}</p>
        </div>
    </div>
        </div>
        </div>
        </div>

      <!-- Footer -->
      <footer class="custom-footer">
        <div class="container" style="display: block;">
          <div class="footer-top">
            <div class="footer-left">
              <img src="/images/logofooter.svg" alt="NewVillage Logo" class="footer-logo-img" />
        </div>
            <div class="footer-right">
              <nav class="footer-links">
                <button id="faqs-trigger" class="footer-link faq-scroll-trigger" style="background:none; border:none; padding:0; cursor:pointer;">{t('footerLinks.faqs')}</button>
                <span class="footer-separator">|</span>
                <a href={`/${lang}/contact`} class="footer-link">{t('footerLinks.contact')}</a>
                <span class="footer-separator">|</span>
                <a href={`/${lang}/legal`} class="footer-link">{t('footerLinks.legal')}</a>
                <span class="footer-separator">|</span>
                <a href={`/${lang}/privacy`} class="footer-link">{t('footerLinks.privacy')}</a>
                <span class="footer-separator">|</span>
                <a href="https://www.instagram.com/newvillagestudio" target="_blank" rel="noopener noreferrer" class="footer-social-link">
                  <svg class="footer-social-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect>
                    <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
                    <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line>
            </svg>
          </a>
              </nav>
        </div>
        </div>

          <div class="footer-bottom">
            <div class="footer-text-absolute">
              <span class="footer-text-main">NEW VILLAGE STUDIO.</span>
              <span class="footer-text-sub">MOVE, MEET & CONNECT</span>
        </div>
        </div>
        </div>
      </footer>
      </div>

    <script>
      // Discipline Detail Toggle
      function toggleDiscipline(disciplineName) {
        const item = document.querySelector(`[data-discipline="${disciplineName}"]`);
        const detail = document.getElementById(`${disciplineName}-detail`);
        
        if (!item || !detail) return;
        
        const isOpen = detail.classList.contains('active');
        const isMobile = window.innerWidth <= 768;

        // Cerrar otros detalles abiertos
        document.querySelectorAll('.discipline-detail').forEach(d => {
          if (d.id !== `${disciplineName}-detail`) {
            d.classList.remove('active');
            if (isMobile) d.style.display = 'none';
          }
        });
        document.querySelectorAll('.discipline-item').forEach(i => {
          if (i.dataset.discipline !== disciplineName) {
            i.classList.remove('active');
          }
        });

        if (isOpen) {
          // Cerrar este detalle
          detail.classList.remove('active');
          if (isMobile) detail.style.display = 'none';
          item.classList.remove('active');
        } else {
          // Abrir este detalle
          detail.classList.add('active');
          item.classList.add('active');
          if (isMobile) detail.style.display = 'flex';
          
          // Scroll suave al detalle (solo en desktop o si queremos en móvil)
          if (!isMobile) {
            setTimeout(() => {
              const detailRect = detail.getBoundingClientRect();
              const absoluteTop = window.pageYOffset + detailRect.top;
              const headerOffset = 80;
              const windowHeight = window.innerHeight;
              const detailHeight = detailRect.height;
              const centerOffset = (windowHeight - detailHeight) / 2;
              const scrollTop = absoluteTop - headerOffset - Math.max(centerOffset, 50);
              window.scrollTo({
                top: Math.max(scrollTop, 0),
                behavior: 'smooth'
              });
            }, 350);
          }
        }
      }

      document.querySelectorAll('.mobile-close-detail').forEach(btn => {
        btn.addEventListener('click', (e) => {
            e.preventDefault();
            e.stopPropagation();
            const detail = btn.closest('.discipline-detail');
            if (detail) {
                const id = detail.id.replace('-detail', '');
                toggleDiscipline(id);
            }
        });
      });

      document.addEventListener('click', function(e) {
        const disciplineItem = e.target.closest('.discipline-item');
        if (!disciplineItem) return;
        
        const disciplineName = disciplineItem.dataset.discipline;
        if (!disciplineName) return;
        
        if (e.target.closest('.discipline-view-more-btn')) {
          e.preventDefault();
          e.stopPropagation();
          toggleDiscipline(disciplineName);
          return;
        }
        
        e.preventDefault();
        toggleDiscipline(disciplineName);
      });

      function handleMobileLayout() {
        const isMobile = window.innerWidth <= 768;
        const container = document.querySelector('.disciplines-container');
        const details = document.querySelectorAll('.discipline-detail');
        
        if (isMobile) {
          details.forEach(detail => {
            const id = detail.id.replace('-detail', '');
            const item = document.querySelector(`.discipline-item[data-discipline="${id}"]`);
            if (item && item.nextElementSibling !== detail) {
               item.after(detail);
            }
          });
        } else {
          const order = ['barre-detail', 'pilates-detail', 'funcional-detail', 'yoga-detail'];
          order.forEach(id => {
            const detail = document.getElementById(id);
            if (detail && detail.parentNode !== container) {
              container.appendChild(detail);
            }
          });
        }
        
        if (typeof updateCarousel === 'function') {
          setTimeout(updateCarousel, 50);
        }
      }

      window.addEventListener('resize', () => {
        handleMobileLayout();
      });
      handleMobileLayout();

      const faqTrigger = document.getElementById('faqs-trigger');
      const faqModal = document.getElementById('faqs-modal');
      const faqClose = document.querySelector('.faqs-modal-close');

      function openFaqModal(e) {
        e.preventDefault();
        faqModal?.classList.add('open');
        document.body.style.overflow = 'hidden';
      }

      function closeFaqModal() {
        faqModal?.classList.remove('open');
        document.body.style.overflow = '';
      }

      faqTrigger?.addEventListener('click', openFaqModal);
      faqClose?.addEventListener('click', closeFaqModal);

      faqModal?.addEventListener('click', (e) => {
        if (e.target === faqModal) {
          closeFaqModal();
        }
      });

      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && faqModal?.classList.contains('open')) {
          closeFaqModal();
        }
      });

      const faqQuestions = document.querySelectorAll('.faq-question');
      
      faqQuestions.forEach(question => {
        question.addEventListener('click', () => {
          const item = question.parentElement;
          const isActive = item.classList.contains('active');
          
          document.querySelectorAll('.faq-item').forEach(i => {
            i.classList.remove('active');
            const icon = i.querySelector('.faq-icon');
            if(icon) icon.style.transform = 'rotate(0deg)';
          });

          if (!isActive) {
            item.classList.add('active');
            const icon = item.querySelector('.faq-icon');
            if(icon) icon.style.transform = 'rotate(180deg)';
          }
        });
      });

      const carouselItems = document.querySelectorAll('.discipline-item');
      let currentCarouselIndex = 0;

      function updateCarousel() {
        const isMobile = window.innerWidth <= 768;
        
        // En móvil: mostrar todos los items (sin carrusel)
        // En desktop: comportamiento normal
        carouselItems.forEach(item => {
          item.style.display = '';
          item.classList.remove('active-slide');
        });
        
        document.querySelectorAll('.discipline-detail').forEach(detail => {
          if (!detail.classList.contains('active')) {
            detail.style.display = isMobile ? 'none' : '';
          }
        });
      }

      window.addEventListener('resize', updateCarousel);
      setTimeout(updateCarousel, 100);
    </script>

  </body>
</html>

<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: 'Oswald', sans-serif;
    overflow-x: hidden;
    background-color: #ffffff;
  }

  .container {
    max-width: 1240px;
    margin: 0 auto;
    padding: 0 2rem;
    position: relative;
    height: 100%;
    display: flex;
    align-items: center;
  }

  @keyframes fadeDown {
    from { opacity: 0; transform: translateY(-20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  .hero-section {
    position: relative;
    width: 100%;
    height: 100vh;
    min-height: 600px;
    overflow: hidden;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .hero-bg-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .hero-content {
    position: relative;
    z-index: 3;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
  }

  .hero-text {
    position: relative;
    z-index: 4;
  }

  .hero-title {
    font-family: 'Oswald', sans-serif;
    color: #C6E1EA;
    text-transform: uppercase;
    letter-spacing: 0.02em;
    margin: 0;
    line-height: 1;
  }

  .hero-line-1 {
    font-weight: 500;
    font-size: 89.6px;
    display: block;
    opacity: 0;
    animation: fadeUp 0.8s ease-out 0.3s forwards;
  }

  .hero-line-2 {
    font-weight: 500;
    font-size: 92.7px;
    display: block;
    opacity: 0;
    animation: fadeUp 0.8s ease-out 0.5s forwards;
  }

  .hero-line-3 {
    font-weight: 500;
    font-size: 109px;
    display: block;
    opacity: 0;
    animation: fadeUp 0.8s ease-out 0.7s forwards;
  }

  .hero-logo-container {
    position: absolute;
    top: 50%;
    right: 0;
    transform: translate(30%, -50%) rotate(90deg);
    transform-origin: center center;
    z-index: 1;
    opacity: 0;
    animation: fadeIn 1s ease-out 0.9s forwards;
  }

  .logo-rotated-svg {
    width: 105vh;
    height: auto;
    display: block;
  }

  .disciplines-section {
    padding: 169px 0 0 0;
    background: #ffffff;
    margin-bottom: 0;
  }

  .disciplines-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .disciplines-title {
    font-family: 'Oswald', sans-serif;
    font-weight: 500;
    font-size: 47px;
    line-height: 85px;
    color: #2c2c2c;
    text-transform: uppercase;
    text-align: center;
    margin: 0 0 1rem 0;
  }

  .disciplines-subtitle {
    font-family: 'Oswald', sans-serif;
    font-weight: 300;
    font-size: 38px;
    line-height: 50px;
    color: #2c2c2c;
    text-transform: uppercase;
    text-align: center;
    max-width: 937px;
    margin: 0 auto 169px auto;
  }

  .disciplines-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
    width: 100%;
    margin-bottom: 0;
    padding-bottom: 0;
  }

  .discipline-item {
    width: 100%;
    overflow: hidden;
    position: relative;
    cursor: pointer;
  }

  .discipline-image {
    width: 100%;
    height: 485px;
    object-fit: cover;
    object-position: center;
    display: block;
    transition: transform 0.3s ease;
  }

  .discipline-image-right {
    object-position: 25% center;
  }

  .discipline-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #A53716;
    opacity: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 2rem;
    transition: opacity 0.3s ease;
    z-index: 1;
  }

  .discipline-item:hover .discipline-overlay,
  .discipline-item.active .discipline-overlay {
    opacity: 0.7;
  }

  .discipline-item:hover .discipline-image,
  .discipline-item.active .discipline-image {
    transform: scale(1.05);
  }

  .discipline-gradient-overlay {
    display: none;
  }

  .discipline-view-more-btn {
    display: none;
  }

  .discipline-overlay-title {
    font-family: 'Oswald', sans-serif;
    font-weight: 500;
    font-size: 45px;
    line-height: 50px;
    color: #ffffff;
    text-transform: uppercase;
    margin: 0 0 1rem 0;
  }

  .discipline-overlay-tags {
    font-family: 'Oswald', sans-serif;
    font-weight: 400;
    font-size: 16px;
    line-height: 24px;
    color: #ffffff;
    text-transform: uppercase;
    margin: 0;
    letter-spacing: 0.02em;
    white-space: nowrap;
  }

  .discipline-detail {
    max-height: 0;
    overflow: hidden;
    opacity: 0;
    transition: max-height 0.4s ease, opacity 0.4s ease, padding 0.4s ease;
    padding: 0;
  }

  .discipline-detail.active {
    max-height: 2000px;
    opacity: 1;
    padding: 6rem 0 4rem 0;
  }

  .discipline-detail-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0;
    text-align: center;
  }

  .discipline-detail-title {
    font-family: 'Oswald', sans-serif;
    font-weight: 500;
    font-size: 60px;
    line-height: 50px;
    color: #2c2c2c;
    text-transform: uppercase;
    margin: 0 auto 54px auto;
    text-decoration: underline;
    text-decoration-color: #ED6D64;
    text-decoration-thickness: 3px;
    text-underline-offset: 0.5rem;
    display: inline-block;
    letter-spacing: 0.05em;
  }

  .discipline-detail-subtitle {
    font-family: 'Oswald', sans-serif;
    font-weight: 500;
    font-size: 32px;
    line-height: 50px;
    color: #2c2c2c;
    text-transform: uppercase;
    margin: 0 auto 0 auto;
    text-align: center;
    letter-spacing: 0.07em;
  }

  .discipline-detail-text {
    font-family: 'Oswald', sans-serif;
    font-weight: 300;
    font-size: 28px;
    line-height: 50px;
    color: #2c2c2c;
    margin: 0 auto 81px auto;
    text-align: center;
    width: 100%;
    max-width: 1200px;
    letter-spacing: 0.05em;
    box-sizing: border-box;
  }

  .discipline-programs {
    display: flex;
    gap: 40px;
    justify-content: space-between;
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
  }

  .program-box {
    flex: 1;
    padding: 32px;
    color: #ffffff;
    display: flex;
    flex-direction: column;
    justify-content: center;
    min-height: 200px;
    box-sizing: border-box;
  }

  .program-box-1 { background-color: #A53716; }
  .program-box-2 { background-color: #DB5639; }
  .program-box-3 { background-color: #ED6D64; }
  .program-box-4 { background-color: #ED6D64; }
  .program-box-5 { background-color: #ED6D64; }

  .discipline-programs-5 .program-box-1 { background-color: #A53716; }
  .discipline-programs-5 .program-box-2 { background-color: #D74220; }
  .discipline-programs-5 .program-box-3 { background-color: #DB5639; }
  .discipline-programs-5 .program-box-4 { background-color: #F85B50; }
  .discipline-programs-5 .program-box-5 { background-color: #ED6D64; }

  #barre-detail .discipline-programs .program-box-1 { background-color: #A53716; }
  #barre-detail .discipline-programs .program-box-2 { background-color: #DB5639; }
  #barre-detail .discipline-programs .program-box-3 { background-color: #ED6D64; }

  #pilates-detail .discipline-programs .program-box-1 { background-color: #A53716; }
  #pilates-detail .discipline-programs .program-box-2 { background-color: #DB5639; }
  #pilates-detail .discipline-programs .program-box-3 { background-color: #ED6D64; }

  .discipline-programs-5 {
    display: flex;
    flex-direction: column;
    gap: 40px;
    align-items: center;
  }

  .discipline-programs-5 .program-row {
    display: flex;
    gap: 40px;
    justify-content: center;
    width: 100%;
  }

  .discipline-programs-5 .program-row-top .program-box {
    flex: 0 0 calc((100% - 80px) / 3);
    width: calc((100% - 80px) / 3);
    max-width: 360px;
    min-width: 280px;
  }

  .discipline-programs-5 .program-row-bottom .program-box {
    flex: 0 0 calc((100% - 40px) / 2);
    width: calc((100% - 40px) / 2);
    max-width: 360px;
    min-width: 280px;
  }

  .discipline-programs-2 {
    display: flex;
    gap: 40px;
    justify-content: center;
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
  }

  .discipline-programs-2 .program-box {
    flex: 0 0 auto;
    width: auto;
    max-width: 360px;
    min-width: 280px;
  }

  .program-box-yoga-1 { background-color: #A53716; }
  .program-box-yoga-2 { background-color: #ED6D64; }

  .program-title {
    font-family: 'Oswald', sans-serif;
    font-weight: 600;
    font-size: 38px;
    line-height: 42px;
    color: #ffffff;
    text-transform: uppercase;
    margin: 0 0 1rem 0;
    letter-spacing: 0.1em;
    word-wrap: break-word;
    overflow-wrap: break-word;
  }

  .program-text {
    font-family: 'Oswald', sans-serif;
    font-weight: 300;
    font-size: 26px;
    line-height: 30px;
    color: #ffffff;
    margin: 0;
    letter-spacing: 0.1em;
    white-space: pre-line;
  }

  .cta-section {
    background-color: #90BBC9;
    height: 450px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem 0;
  }

  .cta-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    gap: 4rem;
    width: 100%;
  }

  .cta-text {
    font-family: 'Oswald', sans-serif;
    font-weight: 600;
    font-size: 48px;
    line-height: 48px;
    color: #ffffff;
    text-transform: uppercase;
    max-width: 1000px;
    margin: 0;
  }

  .cta-button {
    font-family: 'Oswald', sans-serif;
    font-weight: 500;
    font-size: 20px;
    line-height: 40px;
    color: #ffffff;
    text-transform: uppercase;
    text-decoration: none;
    padding: 0.6rem 2rem;
    border: 2px solid #ffffff;
    border-radius: 50px;
    background: transparent;
    transition: all 0.3s ease;
    display: inline-block;
    letter-spacing: 0.05em;
  }

  .cta-button:hover {
    background: #ffffff;
    color: #90BBC9;
  }

  .local-image-section {
    width: 100%;
    height: 600px;
    overflow: hidden;
    margin: 0;
    padding: 0;
  }

  .local-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
    display: block;
  }

  .faqs-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.7);
    z-index: 2000;
    display: flex;
    justify-content: center;
    align-items: center;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
    backdrop-filter: blur(5px);
  }

  .faqs-modal.open {
    opacity: 1;
    visibility: visible;
  }

  .faqs-modal-content {
    background: white;
    padding: 3rem;
    border-radius: 10px;
    width: 90%;
    max-width: 800px;
    max-height: 90vh;
    overflow-y: auto;
    position: relative;
    text-align: center;
    transform: translateY(20px);
    transition: transform 0.3s ease;
  }

  .faqs-modal.open .faqs-modal-content {
    transform: translateY(0);
  }

  .faqs-modal-close {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    background: none;
    border: none;
    cursor: pointer;
    color: #2c2c2c;
    transition: color 0.3s ease;
    padding: 0.5rem;
    z-index: 10;
  }

  .faqs-modal-close:hover {
    color: #ED6D64;
  }

  .faqs-title {
    font-family: 'Oswald', sans-serif;
    font-weight: 500;
    font-size: 48px;
    line-height: 1.2;
    color: #2c2c2c;
    text-transform: uppercase;
    text-align: center;
    margin-bottom: 2.5rem;
    width: 100%;
  }

  .faqs-accordion {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .faq-item {
    border-bottom: 1px solid #eee;
  }

  .faq-question {
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 0;
    background: none;
    border: none;
    cursor: pointer;
    text-align: left;
    transition: color 0.3s ease;
  }

  .faq-question span {
    font-family: 'Oswald', sans-serif;
    font-weight: 400;
    font-size: 24px;
    color: #2c2c2c;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    padding-right: 1rem;
  }

  .faq-question:hover span {
    color: #ED6D64;
  }

  .faq-icon {
    transition: transform 0.3s ease;
    color: #ED6D64;
    flex-shrink: 0;
  }

  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease, padding 0.3s ease;
    padding: 0;
  }

  .faq-item.active .faq-answer {
    max-height: 300px;
    padding-bottom: 1.5rem;
  }

  .faq-answer p {
    font-family: 'Oswald', sans-serif;
    font-weight: 300;
    font-size: 18px;
    line-height: 1.6;
    color: #666;
    margin: 0;
    text-align: left;
  }

  .custom-footer {
    background-color: #ffffff;
    padding: 50px 0 40px 0;
    margin-top: 0;
  }

  .footer-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-bottom: 100px;
  }

  .footer-logo-img {
    height: auto;
    width: 140px;
  }

  .footer-links {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .footer-link {
    font-family: 'Oswald', sans-serif;
    font-weight: 500;
    font-size: 28px;
    line-height: 35px;
    letter-spacing: 2px;
    color: #000000;
    text-decoration: none;
    text-transform: uppercase;
    transition: color 0.3s ease;
  }

  .footer-link:hover {
    color: #ED6D64;
  }

  .footer-separator {
    color: #ED6D64;
    font-size: 28px;
    font-weight: 500;
    margin: 0 4px;
  }

  .footer-social-icon {
    width: 35px;
    height: 35px;
    color: #000000;
    margin-left: 8px;
    transition: color 0.3s ease;
  }

  .footer-social-icon:hover {
    color: #ED6D64;
  }

  .footer-bottom {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 280px;
    width: 100%;
    margin-top: 60px;
    overflow: visible;
  }

  .footer-bottom::before {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100%;
    height: 100%;
    max-width: 1000px;
    background-color: #ED6D64;
    -webkit-mask-image: url('/images/logoicon.svg');
    mask-image: url('/images/logoicon.svg');
    -webkit-mask-repeat: no-repeat;
    mask-repeat: no-repeat;
    -webkit-mask-position: center;
    mask-position: center;
    -webkit-mask-size: contain;
    mask-size: contain;
    opacity: 0.4;
    z-index: 0;
    pointer-events: none;
  }

  .footer-text-absolute {
    position: relative;
    z-index: 10;
    text-align: center;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: row;
    gap: 10px;
  }

  .footer-text-main {
    font-family: 'Oswald', sans-serif;
    font-weight: 500;
    font-size: 26px;
    line-height: 32px;
    letter-spacing: 2px;
    color: #000000;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .footer-text-sub {
    font-family: 'Oswald', sans-serif;
    font-weight: 300;
    font-size: 26px;
    line-height: 32px;
    letter-spacing: 2px;
    color: #000000;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .mobile-only {
    display: none;
  }

  @media (max-width: 1024px) {
    .desktop-only { display: none !important; }
    .mobile-only { display: block !important; }
    .hero-line-1 { font-size: 60px; }
    .hero-line-2 { font-size: 62px; }
    .hero-line-3 { font-size: 72px; }
    .hero-logo-container {
      opacity: 0.2;
      transform: translate(40%, -50%) rotate(90deg);
    }
  }

  @media (max-width: 768px) {
    .container { padding: 0 1.5rem; }
    .hero-line-1 { font-size: 56px; }
    .hero-line-2 { font-size: 58px; }
    .hero-line-3 { font-size: 68px; }
    .logo-rotated-svg { width: 85vh; }
    .logo-rotated-svg path { fill-opacity: 0.5 !important; }
    .hero-logo-container {
      transform: translate(45%, -50%) rotate(90deg);
      opacity: 0.75;
      right: 0;
    }
    
    /* Disciplinas simplificadas para móvil */
    .disciplines-section { padding: 60px 0 60px 0 !important; margin-bottom: 0 !important; }
    .disciplines-title { font-size: 28px; line-height: 1.0; margin-bottom: 0.75rem; }
    .disciplines-subtitle { font-size: 18px; line-height: 1.3; max-width: 100%; margin-bottom: 40px; }
    
    /* Ocultar botones de carrusel */
    .carousel-nav-btn,
    .carousel-prev,
    .carousel-next { display: none !important; }
    
    /* Wrapper sin fondo naranja, sin padding lateral excesivo */
    .disciplines-carousel-wrapper {
      position: relative;
      background-color: transparent;
      padding: 0;
      margin-left: 0;
      width: 100%;
      box-sizing: border-box;
      min-height: auto;
      display: block;
    }
    
    /* Grid como lista vertical simple */
    .disciplines-grid { 
      display: flex !important; 
      flex-direction: column !important; 
      gap: 20px;
      margin-bottom: 0;
    }
    
    /* Cada disciplina visible */
    .discipline-item { 
      width: 100%; 
      display: block !important;
      position: relative;
      border-radius: 8px;
      overflow: hidden;
    }
    
    .discipline-image { 
      height: 280px;
      transform: none !important; /* Sin zoom en móvil */
    }
    
    /* Quitar hover en móvil */
    .discipline-item:hover .discipline-image,
    .discipline-item.active .discipline-image {
      transform: none !important;
    }
    
    .discipline-overlay {
      opacity: 1 !important;
      background-color: rgba(0, 0, 0, 0.4);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 1.5rem;
    }
    
    .discipline-overlay-title { font-size: 28px; line-height: 32px; margin-bottom: 0.5rem; }
    .discipline-overlay-tags { font-size: 12px; line-height: 16px; margin-bottom: 1rem; }
    
    /* Sin cambio de fondo en active en móvil */
    .discipline-item.active .discipline-overlay { background-color: rgba(0, 0, 0, 0.4); }
    
    /* Botón Descubre visible en móvil - bordes redondeados */
    .mobile-only { display: flex; }
    .discipline-view-more-btn.mobile-only {
      background: transparent;
      border: 1px solid white;
      color: white;
      padding: 12px 28px;
      font-family: 'Oswald', sans-serif;
      font-size: 14px;
      font-weight: 400;
      letter-spacing: 1px;
      cursor: pointer;
      text-decoration: none;
      border-radius: 30px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
    }
    
    /* Ocultar gradient overlay en móvil */
    .discipline-gradient-overlay { display: none !important; }
    
    /* Ocultar detalles desplegables en móvil - el botón lleva a planes */
    .discipline-detail { 
      display: none !important; 
    }
    
    /* Ocultar botón cerrar en móvil */
    .mobile-close-detail { display: none !important; }
    
    /* Ocultar programas en móvil */
    .discipline-programs,
    .discipline-programs-5,
    .discipline-programs-2,
    .program-box,
    .program-row { 
      display: none !important; 
    }
    
    .cta-section { height: auto; min-height: 350px; padding: 3rem 1.5rem; }
    .cta-text { font-size: 30px; line-height: 36px; max-width: 100%; padding: 0 1rem; }
    .local-image-section { height: 350px; }
    .custom-footer { padding: 60px 0; margin-top: 0; }
    .footer-top { flex-direction: column; align-items: center; gap: 40px; margin-bottom: 60px; }
    .footer-links { flex-direction: column; gap: 20px; }
    .footer-link { font-size: 24px; line-height: 30px; }
    .footer-separator { display: none; }
    .footer-bottom { min-height: 200px; margin-top: 20px; }
    .footer-text-absolute { flex-direction: column; gap: 0; }
    .footer-text-main, .footer-text-sub { font-size: 24px; line-height: 32px; white-space: normal; }
  }
</style>
